<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:background="@color/dialogBG"
    android:alpha=".8"
    android:layout_height="100dp">

    <RadioButton
        android:id="@+id/pickSelectSetRadioButton"
        app:layout_constraintRight_toLeftOf="@+id/saveItemNameTextView"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/saveItemScrollingText"
        app:layout_constraintTop_toTopOf="parent"
        android:text="Select"
        android:padding="5dp"
        android:textColor="@color/baseText"
        android:textSize="18sp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <TextView
        android:id="@+id/saveItemNameTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Save Name"
        android:textColor="@color/baseText"
        android:textSize="24sp"
        app:layout_constraintBottom_toTopOf="@id/saveItemScrollingText"
        app:layout_constraintLeft_toRightOf="@id/pickSelectSetRadioButton"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/saveItemScrollingText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="marquee"
        android:marqueeRepeatLimit="marquee_forever"
        android:padding="5dp"
        android:scrollHorizontally="true"
        android:singleLine="true"
        android:text=""
        android:textColor="@color/baseText"
        android:textSize="18sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/saveItemNameTextView" />

</androidx.constraintlayout.widget.ConstraintLayout>